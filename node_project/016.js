// 這裡你會需要用到import方法導入datetime模塊。datatime模塊重新封裝了time模塊，提供更多接口，提供的類有：date,time,datetime,timedelta,tzinfo

// datetime.date(year, month, day)

// date.max、date.min：
// date對象所能表示的最大、最小日期；

// date.resolution：
// date對象表示日期的最小單位（天）。

// date.today()：
// 返回一個表示當前本地日期的date對象；

// date.fromtimestamp(timestamp)：
// 根據給定的時間戮，返回一個date對象；